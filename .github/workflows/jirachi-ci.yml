name: Jirachi CI

on:
  push:
    paths:
      - 'jirachi/**'
      - 'database/**'
      - '.github/workflows/jirachi-ci.yml'

jobs:
  lint-test:
    name: Lint & Test jirachi
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.4'

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Install gosec for SAST
        run: go install github.com/securego/gosec/v2/cmd/gosec@latest

      - name: Install gotestfmt
        run: go install github.com/gotesttools/gotestfmt/v2/cmd/gotestfmt@latest

      # All subsequent Go-related commands are run from the jirachi directory
      - name: Change to jirachi directory
        run: cd jirachi
        working-directory: .

      - name: Verify Go Modules (go mod tidy)
        working-directory: ./jirachi
        run: |
          go mod tidy
          # Fail if go.mod or go.sum differs after tidy
          git diff --exit-code go.mod go.sum

      - name: Run govulncheck for vulnerability scanning
        working-directory: ./jirachi
        run: govulncheck ./...

      - name: Run go vet
        working-directory: ./jirachi
        run: go vet ./...

      - name: Run gosec SAST scan
        working-directory: ./jirachi
        run: gosec -exclude-dir=queries ./...

      - name: Lint Go code (golangci-lint)
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.1
          working-directory: ./jirachi
          args: --fast-only

      - name: Run unit tests
        working-directory: ./jirachi
        run: make test-unit